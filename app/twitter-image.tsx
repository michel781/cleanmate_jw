// Twitter card uses the exact same image as Open Graph.
// Re-exporting from opengraph-image keeps a single source of truth for the design.
export { default, alt, size, contentType } from './opengraph-image';
